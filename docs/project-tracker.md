# Quiet Room Mobile Project Tracker

This document is a living tracker for mobile app follow-up work, investigations, and future improvements.

## Status key

- `Backlog`
- `Investigating`
- `Ready`
- `In Progress`
- `Done`
- `Blocked`

## Current tracker

| ID | Status | Priority | Area | Item | Notes / next step |
| --- | --- | --- | --- | --- | --- |
| QR-MOB-001 | Done | High | Production logs / retention | Confirm whether production logs have a 90-day TTL and update retention if they do not. | Verified on 2026-05-21. Prod Lambda log groups were updated from `Never expire` to 90 days. |
| QR-MOB-002 | In Progress | High | Android/iOS UI | Fix mobile composer spacing around the keyboard and bottom safe area. | Android/iOS revision is merged to `develop` at `b7ed619` and both QA store lanes have uploaded. Android QA is deployed to Play internal as draft release `QA internal 12` through Play edit `15087252310441301604` (`com.quietroom.mobile.qa`, versionCode `12`, AAB SHA256 `905a4ca06a409af75fa79fb4de91c281a103f45e19fc137b3dee3ca3eb8e4a85`). iOS QA build `20` uploaded to App Store Connect/TestFlight on 2026-05-23 after restoring Xcode account access; upload output reported `Upload succeeded`, `Uploaded QuietRoomQA`, and `** EXPORT SUCCEEDED **`. The calculation removes the extra Android `+96` footer lift, subtracts the bottom system inset once from the reported IME height so Pixel gesture nav and Galaxy 3-button nav do not drift apart, and uses only `4px` extra keyboard-open footer clearance (`20px` total with base padding). Pixel evidence `docs/qr-mob-002-android-keyboard-spacing/evidence/pixel34-keyboard-equalized-final.png` and Galaxy evidence `docs/qr-mob-002-android-keyboard-spacing/evidence/galaxy-s22-keyboard-equalized-final.png` show the composer directly above the Gboard suggestion strip with no yellow/beige band. iOS scope was added after user review: the main screen now uses top-only safe-area edges so the resting beige band below the composer is structurally removed, with `36px` total resting bottom padding to match the voice-mode badge spacing. |
| QR-MOB-003 | Backlog | Medium | Store listing / age rating | Investigate whether the app age range/rating needs to change. | Review Google Play and Apple age-rating questionnaires against actual app behavior and AI companion content. |
| QR-MOB-004 | In Progress | High | QA feedback / chat reliability | Review QA feedback about timestamp bleed-through, strange context appearing, and missing responses. | Timestamp sanitizer and internal-output retry protections deployed to QA. Continue monitoring reports. |
| QR-MOB-005 | Backlog | Medium | Profile / user control | Consider putting the profile feature behind a feature flag so users can turn it off and on. | Clarify frontend/backend behavior when disabled and whether users can delete/reset profile state. |
| QR-MOB-006 | Done | Medium | Models / long-term AI strategy | Investigate longer-term model strategy beyond current OpenAI models, including possible local or non-OpenAI options. | Completed for the backend/mobile QA scope. |
| QR-MOB-007 | In Progress | Medium | Chat UX / message selection | Investigate making chat message text selectable in addition to the copy icon. | Branch `codex/qr-mob-007-selectable-message-text` enables native text selection. |
| QR-MOB-008 | Done | High | Profile builder / model evals | Evaluate `gpt-5.5` for the profile-building prompt and memory/profile generation pipeline. | Completed. |
| QR-MOB-009 | Done | High | Profile system / split-profile evaluation | Evaluate how the split-profile system is performing after several months of real usage. | Completed. |
| QR-MOB-010 | Done | High | Feedback / privacy consent | Implement explicit report-content consent for the feedback/report flow. | Completed. |
| QR-MOB-011 | Backlog | Medium | Accounts / anonymous users | Evaluate anonymous-user lifecycle, mobile session persistence, cleanup needs, and intended upgrade or retention flow. | Review mobile vs web anonymous auth behavior. |
| QR-MOB-012 | Backlog | High | iOS sign-in / Firebase QA parity | Align QA Firebase/Firestore iOS sign-in configuration with prod so Apple sign-in behavior matches across environments. | Verify QA iOS sign-in end to end after changes. |
| QR-MOB-013 | Backlog | Medium | Architecture / data storage | Evaluate whether Quiet Room should continue using Firestore or move more storage into an AWS-native solution. | Compare Firestore vs AWS options. |
| QR-MOB-014 | Backlog | Medium | Observability / CloudWatch reporting | Investigate automating CloudWatch Logs Insights reports for prod usage, prod errors, and QA errors, with scheduled email delivery. | Define recurring queries and delivery mechanism. |
| QR-MOB-015 | Backlog | High | Android deploy / Play Store automation | Investigate fully automating Play Store release publishing. | Review Play API edit flow and publishing automation. |
| QR-MOB-016 | Backlog | High | Voice / audio reliability | Investigate audio playback cutting off mid-message and add visibility into the actual text sent to voice/TTS. | Users sometimes hear audio stop partway through a response while the backend does not show an obvious error. |
| QR-MOB-017 | Done | High | QA feedback / Sonnet investigation | Investigate Sonnet timestamp leakage and temporal reasoning failures using real QA reports. | Completed. |
| QR-MOB-018 | In Progress | High | Profile system / model evals | Evaluate split-profile generation quality with `gpt-5.5` and Anthropic Sonnet 4.6. | In progress. |
| QR-MOB-019 | Ready | High | Profile system / QA rollout | Validate the QA split-profile freshness prompt changes before promoting QA backend to prod. | QA validation pending. |
| QR-MOB-020 | Done | High | iOS QA / TestFlight recovery | Investigate and recover from iOS QA TestFlight build `25` failure. | Completed. |
| QR-MOB-021 | In Progress | High | Voice / streaming playback diagnostics | Build a reproducible mobile clipping test harness and evaluate alternative playback implementations. | Isolated in worktree `../worktrees/quiet-room-mobile-qr-mob-021-voice-playback-diagnostics` on branch `codex/qr-mob-021-voice-playback-diagnostics`; ongoing notes live in `docs/qr-mob-021-voice-playback-diagnostics/progress.md`. Android QA internal `versionCode 20` was uploaded as `QA internal 20` with the in-app voice diagnostics entry gated to Firebase UID `b71cO4Azg8Sx2YofK5UFblMLCMk2`. The diagnostics screen opens from the normal chat flow, pre-fills the current conversation ID and latest assistant message index, and compares `expo-av` versus `expo-audio` against the same saved assistant message. Prior QA/newuser Android flow completed normal voice playback and one-run diagnostics for both engines with `0` clipped attempts. As an incremental production-path mitigation, `develop` now uses `expo-audio` instead of `expo-av` for normal message voice playback; QA store deployment completed on 2026-06-13 with QuietRoomQA TestFlight build `28` uploaded to App Store Connect and Android Play internal draft release `QA internal 21` through Play edit `15766180095082070959` (`com.quietroom.mobile.qa`, versionCode `21`, AAB SHA256 `80acb2e699c6329d607249ba10866e2de24324a2eeab237ba3ab50d6ac7c4fec`). Continue real-device monitoring for streaming clipping reports on the QA build. |
| QR-MOB-022 | Backlog | High | Apple release / App Store production | Prepare and submit the first production App Store release. | Review `docs/privacy-v2/` deliverables and `docs/privacy/store-console-disclosure-worksheet.md`. Create final App Store screenshots using production branding and content, complete App Store metadata/reviewer notes, verify production privacy-manifest packaging, verify privacy/support/account-deletion links and disclosures, run a production-store-candidate smoke test (anonymous chat, AI consent, Apple sign-in, report response, account deletion, voice playback), and submit the production build for App Review. Success criteria: App Store Connect listing is complete, screenshots uploaded, disclosures reviewed, production build validated, and the app is submitted for Apple review. |
| QR-MOB-023 | Done | High | iOS UI / safe area | Investigate iOS modal close button safe area issue. | Fixed in worktree `quiet-room-mobile-qr-mob-023-024-modal-keyboard` / branch `codex/qr-mob-023-024-modal-keyboard` by applying safe-area top/bottom padding to fullscreen modals and preserving touchable close controls below the status area. Emulator verification passed on 2026-06-12 with `E2E_API_BASE=http://localhost:5002 bash ./scripts/with-mobile-env.sh qa local npx detox test -c ios.sim.release e2e/quiet-room.crucifix-modal.test.js e2e/quiet-room.report-response.test.js --record-logs all --take-screenshots all` and the matching Android command using `android.att.release`. Evidence: iOS close frame `y=74`, Android close frame `y=137`; screenshots in `artifacts/ios.sim.release.2026-06-12 01-35-29Z/` and `artifacts/android.att.release.2026-06-12 01-36-02Z/`. Requested iOS/Android emulator verification is complete; no physical-device pass was run in this update. Merged to `develop` and uploaded to App Store Connect/TestFlight as QuietRoomQA build `27` on 2026-06-12; upload output reported `Uploaded QuietRoomQA` and `** EXPORT SUCCEEDED **`. |
| QR-MOB-024 | Done | High | Feedback UX / keyboard handling | Fix feedback form notes input being covered by the keyboard. | Fixed in worktree `quiet-room-mobile-qr-mob-023-024-modal-keyboard` / branch `codex/qr-mob-023-024-modal-keyboard` by making the report modal keyboard-aware, safe-area-aware, and scrollable, with the notes field scrolled into view on focus while footer actions remain tappable. Also hardened the local QA Detox login path and report-flow E2E coverage. Emulator verification passed on 2026-06-12 with the paired iOS/Android Detox commands listed in QR-MOB-023. Evidence: iOS note frame `y=362 height=90`, submit frame `y=475`; Android note frame `y=542 height=242`, submit frame `y=841`; screenshots in `artifacts/ios.sim.release.2026-06-12 01-35-29Z/` and `artifacts/android.att.release.2026-06-12 01-36-02Z/`. Merged to `develop` and included in QuietRoomQA TestFlight build `27` uploaded on 2026-06-12. |
| QR-MOB-025 | Done | High | Backend production rollout / model defaults | Audit and execute the Gabriel QA-to-prod backend rollout. | Completed on 2026-06-18. Backend QA branch `develop-from-main` at `a70292e` was promoted to production `main` with merge commit `f191fd8` (`Promote QA backend to production`). Verification before deploy: focused Sonnet/default/profile-builder suite `61 passed, 11 skipped`; broad backend suite excluding the known `FIREBASE_SERVICE_ACCOUNT_JSON` collection test `114 passed, 18 skipped`. Prod deploy proof: `./deploy-prod.sh` built and pushed `054769575180.dkr.ecr.us-east-1.amazonaws.com/gabriel-backend-prod:f191fd8`, and `gabriel_lambda_prod`, `gabriel-profile-builder_prod`, and `gabriel_streaming_lambda_prod` all report `Active`, `LastUpdateStatus=Successful`, and image `gabriel-backend-prod:f191fd8`. Prod env readiness: `ANTHROPIC_API_KEY` present and `ANTHROPIC_MAX_TOKENS=4096` on all three prod Lambdas; `gabriel-profile-builder_prod` has `PROFILE_BUILDER_PROVIDER=anthropic`, `PROFILE_BUILDER_MODEL=claude-sonnet-4-6`, and `PROFILE_BUILDER_ALLOW_EMPTY_CORE=true`. All three prod `/health` checks returned HTTP 200. |
| QR-MOB-026 | Backlog | Medium | Web frontend / model catalog | Align the web frontend model picker with the backend model catalog and defaults. | The web app currently hard-codes GPT model options and sends `model` on every chat request, which can override backend defaults such as Sonnet 4.6. Future work: have the web frontend consume the backend model catalog/default keys, include Anthropic/Sonnet labels and availability, normalize stale conversation selections against the backend-enabled model list, and preserve a clean picker experience when one or multiple models are available. This is not required for the immediate backend QA deploy, but should happen before relying on backend defaults alone for web production behavior. |
| QR-MOB-027 | In Progress | High | Production rollout / split profile | Coordinate the full production rollout across backend, mobile, and split-profile flags. | Backend production deploy is complete at Gabriel `main` commit `f191fd8`; prod Anthropic env readiness and all three prod `/health` checks are verified. Mobile production deploy completed on 2026-06-18 from source commit `98b99f5`: `origin/master` was promoted, Quiet Room iOS build `28` uploaded to App Store Connect/TestFlight with `Uploaded QuietRoom` and `** EXPORT SUCCEEDED **`, and Android production `versionCode 21` uploaded to Google Play internal as draft release `PROD internal 21` through Play edit `04628924593668462994` (`com.quietroom.mobile`, AAB SHA256 `e00c9c96df522f0a4a2a1850ebfbcb79c6c2d575978cada8888f5fc4229cfd5f`, readback `versionCodes ["21"]`, `status draft`). Corrective mobile redeploy for the stale GPT-5.1 model option is complete at `origin/master` commit `408b35d`: mobile now filters deprecated `gpt-5.1-chat-latest`, falls back to `claude-sonnet-4-6`, verified both iOS and Android production-config Detox response smokes, uploaded Quiet Room iOS build `29` to App Store Connect/TestFlight with `Uploaded QuietRoom` and `** EXPORT SUCCEEDED **`, and uploaded Android production `versionCode 22` to Google Play internal as draft release `PROD internal 22` through Play edit `16929609920968898644` (`com.quietroom.mobile`, AAB SHA256 `83a90c97118a78e8550dc964829753ccde15b86be3e09caa62d90a4ab73bf85e`, readback `versionCodes ["22"]`, `status draft`). Corrective mobile redeploy for the model-switch persistence issue is complete at `origin/master` commit `1d6212d`: mobile now syncs `logicalModelKey` with `currentModel` so stale logical routes cannot snap the picker back; iOS build `30` uploaded to App Store Connect/TestFlight with `Uploaded QuietRoom` and `** EXPORT SUCCEEDED **`, and Android production `versionCode 23` uploaded to Google Play internal as draft release `PROD internal 23` through Play edit `16564828441927027838` (`com.quietroom.mobile`, AAB SHA256 `21ac580d8e358fd61a22bff7c85478364db8be5e33b60df81e83976f05354dbe`, readback `versionCodes ["23"]`, `status draft`). Prod split-profile flags `new_profile_memory_write` and `new_profile_memory_read` are enabled for all users in `gabriel-e6156`; live prod verification for `newuser@example.com` confirmed both evaluate `enabled_for_all`, profile builder completed split-memory generation for `qr-mob-027-prod-proof-1781821333766`, Firestore split docs exist, and follow-up chat `qr-mob-027-prod-split-read-1781821424122` logged `profile_source=split_memory`. Remaining work: full installed-build mobile smoke, App Store review submission / public production release, and Play rollout beyond the internal draft release. |
